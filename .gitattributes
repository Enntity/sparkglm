# Unified-diff context lines intentionally contain a single prefix space.
# Preserve the audited patch bytes; the installed source has its own hash gate.
patches/video/*.patch -whitespace
research/atlas/nvidia-modelopt-refresh.patch -whitespace
research/atlas/nvidia-prefill-mtp2.patch -whitespace
research/atlas/nvidia-prefill-rejected-standalones.patch -whitespace
# Preserve checksum-bound raw Prometheus receipts, including upstream HELP spaces.
results/candidates/*/raw/*.prom -whitespace
