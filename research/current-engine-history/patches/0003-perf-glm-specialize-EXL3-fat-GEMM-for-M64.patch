From ab378440806ddeb05736ee6972e40639e7d08b93 Mon Sep 17 00:00:00 2001
From: Jason McCartney <jmacAJ@users.noreply.github.com>
Date: Thu, 3 Sep 2026 02:53:50 -0700
Subject: [PATCH 03/21] perf(glm): specialize EXL3 fat GEMM for M64

Co-authored-by: OpenAI Codex <codex@openai.com>
---
 Dockerfile                       |   2 +-
 Dockerfile.exl3-derivative       |   2 +-
 overlay/exl3.py                  |  59 ++++++++++++-
 overlay/exl3_fat_gemm.cu         | 137 ++++++++++++++++++++++++-------
 overlay/exl3_fat_gemm.cuh        |  31 +++++++
 overlay/patch_exl3_fat_kernel.py |   5 +-
 tests/test_exl3_overlay.py       |  38 +++++++++
 7 files changed, 239 insertions(+), 35 deletions(-)

diff --git a/Dockerfile b/Dockerfile
index 6feed18..dad0e26 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -428,7 +428,7 @@ RUN set -eux; \
     cd /tmp/exllamav3; \
     TORCH_CUDA_ARCH_LIST=12.1a MAX_JOBS=8 \
       pip install --no-deps --no-build-isolation --no-cache-dir .; \
-    python3 -c "import torch; import exllamav3_ext; assert hasattr(exllamav3_ext, 'exl3_moe'), dir(exllamav3_ext); assert hasattr(exllamav3_ext, 'exl3_fat_gemm'), dir(exllamav3_ext); assert hasattr(exllamav3_ext, 'exl3_fat_gemm_pair'), dir(exllamav3_ext); assert hasattr(exllamav3_ext, 'exl3_fat_swiglu_had'), dir(exllamav3_ext); assert hasattr(exllamav3_ext, 'exl3_fat_gemm_scatter'), dir(exllamav3_ext); print('exllamav3_ext', exllamav3_ext.__file__, 'exl3_moe=yes fat_gemm=yes fat_pair=yes fused_act=yes')"; \
+    python3 -c "import torch; import exllamav3_ext; assert hasattr(exllamav3_ext, 'exl3_moe'), dir(exllamav3_ext); assert hasattr(exllamav3_ext, 'exl3_fat_gemm'), dir(exllamav3_ext); assert hasattr(exllamav3_ext, 'exl3_fat_gemm_pair'), dir(exllamav3_ext); assert hasattr(exllamav3_ext, 'exl3_fat_swiglu_had'), dir(exllamav3_ext); assert hasattr(exllamav3_ext, 'exl3_fat_gemm_scatter'), dir(exllamav3_ext); assert hasattr(exllamav3_ext, 'exl3_fat_gemm_m64'), dir(exllamav3_ext); assert hasattr(exllamav3_ext, 'exl3_fat_gemm_pair_m64'), dir(exllamav3_ext); assert hasattr(exllamav3_ext, 'exl3_fat_gemm_scatter_m64'), dir(exllamav3_ext); print('exllamav3_ext', exllamav3_ext.__file__, 'exl3_moe=yes fat_gemm=yes fat_pair=yes fused_act=yes m64=yes')"; \
     rm -rf /tmp/exllamav3 /root/.cache/pip
 
 # Keep this AFTER the CUDA compile layer so Python-only hook edits do not
diff --git a/Dockerfile.exl3-derivative b/Dockerfile.exl3-derivative
index 2078d4c..f9ddd00 100644
--- a/Dockerfile.exl3-derivative
+++ b/Dockerfile.exl3-derivative
@@ -27,7 +27,7 @@ RUN set -eux; \
     cd /tmp/exllamav3; \
     TORCH_CUDA_ARCH_LIST=12.1a MAX_JOBS=8 \
       pip install --no-deps --no-build-isolation --no-cache-dir .; \
-    python3 -c "import exllamav3_ext; assert hasattr(exllamav3_ext, 'exl3_fat_gemm_pair'), dir(exllamav3_ext); assert hasattr(exllamav3_ext, 'exl3_fat_swiglu_had'), dir(exllamav3_ext); print(exllamav3_ext.__file__)"; \
+    python3 -c "import exllamav3_ext; assert hasattr(exllamav3_ext, 'exl3_fat_gemm_pair'), dir(exllamav3_ext); assert hasattr(exllamav3_ext, 'exl3_fat_swiglu_had'), dir(exllamav3_ext); assert hasattr(exllamav3_ext, 'exl3_fat_gemm_m64'), dir(exllamav3_ext); assert hasattr(exllamav3_ext, 'exl3_fat_gemm_pair_m64'), dir(exllamav3_ext); assert hasattr(exllamav3_ext, 'exl3_fat_gemm_scatter_m64'), dir(exllamav3_ext); print(exllamav3_ext.__file__)"; \
     rm -rf /tmp/exllamav3 /root/.cache/pip
 
 COPY overlay/exl3.py /usr/local/lib/python3.12/dist-packages/vllm/model_executor/layers/quantization/exl3.py
diff --git a/overlay/exl3.py b/overlay/exl3.py
index e96d0f1..031cf56 100644
--- a/overlay/exl3.py
+++ b/overlay/exl3.py
@@ -180,6 +180,14 @@ def fat_kernel_enabled() -> bool:
     return os.environ.get("EXL3_FAT_KERNEL", "0") != "0"
 
 
+def fat_tile_m() -> int:
+    """Row tile for the direct E2 GEMMs; only compiled variants are valid."""
+    value = int(os.environ.get("EXL3_FAT_TILE_M", "64"))
+    if value not in (64, 128):
+        raise ValueError("EXL3_FAT_TILE_M must be 64 or 128")
+    return value
+
+
 def fat_pair_enabled() -> bool:
     """Read gate/up trellises directly instead of packing them per expert."""
     return os.environ.get("EXL3_FAT_PAIR", "1") != "0"
@@ -229,6 +237,19 @@ def exl3_fat_feature_symbols() -> tuple[bool, bool]:
     )
 
 
+def exl3_fat_m64_symbols() -> tuple[bool, bool, bool]:
+    """Direct, paired, and scatter symbols for the 64-row E2 variant."""
+    try:
+        ext = load_exllamav3_ext()
+    except Exception:
+        return False, False, False
+    return (
+        hasattr(ext, "exl3_fat_gemm_m64"),
+        hasattr(ext, "exl3_fat_gemm_pair_m64"),
+        hasattr(ext, "exl3_fat_gemm_scatter_m64"),
+    )
+
+
 def exl3_device_capability() -> tuple[int, int]:
     """CUDA capability of the current device; (-1, -1) without a GPU."""
     if not torch.cuda.is_available():
@@ -259,6 +280,7 @@ def resolve_exl3_fat_tier(
     shared_suh: bool,
     symbols: tuple[bool, bool, bool] | None = None,
     feature_symbols: tuple[bool, bool] | None = None,
+    m64_symbols: tuple[bool, bool, bool] | None = None,
 ) -> tuple[str, str]:
     """Map the configured fat tier onto what this image + checkpoint can run.
 
@@ -309,6 +331,27 @@ def resolve_exl3_fat_tier(
                 + "; disable the matching EXL3_FAT_* flag or serve the "
                 "matching extension image"
             )
+        if fat_tile_m() == 64:
+            if m64_symbols is None:
+                m64_symbols = exl3_fat_m64_symbols()
+            missing_m64 = [
+                name
+                for name, present in zip(
+                    (
+                        "exl3_fat_gemm_m64",
+                        "exl3_fat_gemm_pair_m64",
+                        "exl3_fat_gemm_scatter_m64",
+                    ),
+                    m64_symbols,
+                )
+                if not present
+            ]
+            if missing_m64:
+                raise RuntimeError(
+                    "EXL3_FAT_TILE_M=64 requires exllamav3_ext."
+                    + "/".join(missing_m64)
+                    + "; use EXL3_FAT_TILE_M=128 or serve the matching image"
+                )
     return configured, f"{configured}_ok"
 
 
@@ -342,6 +385,7 @@ def _exl3_fat_diag_line() -> str:
         f"sym_fat_swiglu_had={int(d['sym_fat_swiglu_had'])}",
         f"fat_pair_enabled={int(d['fat_pair_enabled'])}",
         f"fat_fused_activation_enabled={int(d['fat_fused_activation_enabled'])}",
+        f"fat_tile_m={fat_tile_m()}",
         f"cap={d['cap_major']}.{d['cap_minor']}",
         f"cap_ok={int(d['cap_ok'])}",
         f"tp_rank={d['tp_rank']} tp_size={d['tp_size']}",
@@ -892,6 +936,15 @@ def apply_exl3_batched_fat(
 ) -> torch.Tensor:
     """Run fat experts with persistent buffers and optional direct trellis GEMM."""
     ext = load_exllamav3_ext()
+    tile_m = fat_tile_m()
+    if tile_m == 64:
+        fat_gemm = ext.exl3_fat_gemm_m64
+        fat_gemm_pair = ext.exl3_fat_gemm_pair_m64
+        fat_gemm_scatter = ext.exl3_fat_gemm_scatter_m64
+    else:
+        fat_gemm = ext.exl3_fat_gemm
+        fat_gemm_pair = ext.exl3_fat_gemm_pair
+        fat_gemm_scatter = ext.exl3_fat_gemm_scatter
     offset = 0
     for e, n_rows in enumerate(counts_host):
         start = offset
@@ -924,7 +977,7 @@ def apply_exl3_batched_fat(
                         "EXL3_FAT_PAIR=1 requires "
                         "exllamav3_ext.exl3_fat_gemm_pair"
                     )
-                ext.exl3_fat_gemm_pair(
+                fat_gemm_pair(
                     h13,
                     gate.trellis,
                     up.trellis,
@@ -944,7 +997,7 @@ def apply_exl3_batched_fat(
                 svh13 = scratch["svh13"]
                 svh13[:intermediate].copy_(gate.svh)
                 svh13[intermediate:].copy_(up.svh)
-                ext.exl3_fat_gemm(
+                fat_gemm(
                     h13,
                     packed13,
                     gate_up,
@@ -993,7 +1046,7 @@ def apply_exl3_batched_fat(
                     "EXL3_FAT_KERNEL=1 requires "
                     "exllamav3_ext.exl3_fat_gemm_scatter"
                 )
-            ext.exl3_fat_gemm_scatter(
+            fat_gemm_scatter(
                 h2,
                 down.trellis,
                 out,
diff --git a/overlay/exl3_fat_gemm.cu b/overlay/exl3_fat_gemm.cu
index 67ac455..fc5c615 100644
--- a/overlay/exl3_fat_gemm.cu
+++ b/overlay/exl3_fat_gemm.cu
@@ -13,9 +13,9 @@ namespace {
 
 constexpr int FAT_THREADS = 256;
 constexpr int FAT_TILE_M = 128;
+constexpr int FAT_TILE_M_SMALL = 64;
 constexpr int FAT_TILE_K = 16;
 constexpr int FAT_TILE_N = 128;
-constexpr int FAT_M_BLOCKS = FAT_TILE_M / 16;
 constexpr int FAT_N_BLOCKS = FAT_TILE_N / 16;
 constexpr int FAT_PACKED_WORDS = 4 * 16;
 constexpr float HAD_SCALE = 0.088388347648f;
@@ -117,7 +117,7 @@ __device__ inline void fat_had_ff_128(
     reinterpret_cast<float4*>(output_ptr)[lane] = v;
 }
 
-template <bool scatter, bool paired>
+template <int tile_m, bool scatter, bool paired>
 __global__ __launch_bounds__(FAT_THREADS)
 void exl3_fat_gemm_kernel(
     const half* __restrict__ a,
@@ -133,6 +133,8 @@ void exl3_fat_gemm_kernel(
     int size_n,
     int out_stride_n)
 {
+    static_assert(tile_m == FAT_TILE_M || tile_m == FAT_TILE_M_SMALL);
+    constexpr int tile_m_blocks = tile_m / 16;
     extern __shared__ unsigned char shared_raw[];
     // S2b: 3-stage cp.async pipeline. Stages cycle so that issue(j+2) targets
     // the buffer compute(j) is done with, and sync(j+1) certifies every warp
@@ -141,14 +143,14 @@ void exl3_fat_gemm_kernel(
     constexpr int FAT_STAGES = 3;
     half* sh_a = reinterpret_cast<half*>(shared_raw);
     uint16_t* sh_b = reinterpret_cast<uint16_t*>(
-        sh_a + FAT_STAGES * FAT_TILE_M * FAT_TILE_K);
+        sh_a + FAT_STAGES * tile_m * FAT_TILE_K);
     float* sh_c = reinterpret_cast<float*>(
         sh_b + FAT_STAGES * FAT_N_BLOCKS * FAT_PACKED_WORDS);
 
     int t = threadIdx.x;
     int warp = t / 32;
     int lane = t & 31;
-    int m_base = blockIdx.y * FAT_TILE_M;
+    int m_base = blockIdx.y * tile_m;
     int n_base = blockIdx.x * FAT_TILE_N;
     int out_n_base = n_base;
     if constexpr (paired)
@@ -163,9 +165,9 @@ void exl3_fat_gemm_kernel(
     }
     int tiles_n = size_n / 16;
 
-    FragC frag_c[FAT_M_BLOCKS][2];
+    FragC frag_c[tile_m_blocks][2];
     #pragma unroll
-    for (int mb = 0; mb < FAT_M_BLOCKS; ++mb)
+    for (int mb = 0; mb < tile_m_blocks; ++mb)
     {
         frag_c[mb][0] = {};
         frag_c[mb][1] = {};
@@ -178,20 +180,23 @@ void exl3_fat_gemm_kernel(
     // B: first 64 threads, one int4 each.
     auto issue_stage = [&](int k_block, int stage)
     {
-        int a_row = t / 2;
-        int a_col8 = t & 1;
-        int a_dst_col8 = a_col8 ^ ((a_row >> 2) & 1);
-        int4* a_dst = reinterpret_cast<int4*>(
-            sh_a + stage * FAT_TILE_M * FAT_TILE_K) + a_row * 2 + a_dst_col8;
-        if (m_base + a_row < size_m)
+        if (t < 2 * tile_m)
         {
-            const int4* a_src = reinterpret_cast<const int4*>(
-                a + (m_base + a_row) * size_k + k_block * FAT_TILE_K);
-            cp_async(a_dst, a_src + a_col8);
-        }
-        else
-        {
-            *a_dst = int4{};
+            int a_row = t / 2;
+            int a_col8 = t & 1;
+            int a_dst_col8 = a_col8 ^ ((a_row >> 2) & 1);
+            int4* a_dst = reinterpret_cast<int4*>(
+                sh_a + stage * tile_m * FAT_TILE_K) + a_row * 2 + a_dst_col8;
+            if (m_base + a_row < size_m)
+            {
+                const int4* a_src = reinterpret_cast<const int4*>(
+                    a + (m_base + a_row) * size_k + k_block * FAT_TILE_K);
+                cp_async(a_dst, a_src + a_col8);
+            }
+            else
+            {
+                *a_dst = int4{};
+            }
         }
         if (t < 64)
         {
@@ -227,21 +232,21 @@ void exl3_fat_gemm_kernel(
         dq_dispatch<4, 1>(warp_b, lane << 3, frag_b0, frag_b1);
 
         #pragma unroll
-        for (int mb = 0; mb < FAT_M_BLOCKS; ++mb)
+        for (int mb = 0; mb < tile_m_blocks; ++mb)
         {
             FragA frag_a;
             int row = (lane % 8) + 8 * ((lane / 8) % 2) + mb * 16;
             int base_col = lane / 16;
             int swizzled_col = base_col ^ ((row >> 2) & 1);
             ldsm4(frag_a, reinterpret_cast<int4*>(
-                sh_a + stage * FAT_TILE_M * FAT_TILE_K) + row * 2 + swizzled_col);
+                sh_a + stage * tile_m * FAT_TILE_K) + row * 2 + swizzled_col);
             ptx_mma_m16n8k16(frag_a, frag_b0, frag_c[mb][0]);
             ptx_mma_m16n8k16(frag_a, frag_b1, frag_c[mb][1]);
         }
     }
 
     #pragma unroll
-    for (int mb = 0; mb < FAT_M_BLOCKS; ++mb)
+    for (int mb = 0; mb < tile_m_blocks; ++mb)
     {
         int rows = min(16, size_m - (m_base + mb * 16));
         if (rows <= 0) break;
@@ -332,7 +337,7 @@ void check_common(
                 "exl3_fat_gemm tensors must share a device");
 }
 
-template <bool scatter, bool paired = false>
+template <int tile_m, bool scatter, bool paired = false>
 void launch(
     at::Tensor a,
     at::Tensor packed,
@@ -350,13 +355,13 @@ void launch(
     int size_n = static_cast<int>(svh.numel());
     dim3 block(FAT_THREADS);
     int out_stride_n = paired ? 2 * size_n : size_n;
-    dim3 grid(out_stride_n / FAT_TILE_N, (size_m + FAT_TILE_M - 1) / FAT_TILE_M);
+    dim3 grid(out_stride_n / FAT_TILE_N, (size_m + tile_m - 1) / tile_m);
     // S2b: 3 pipeline stages of (A tile + B tile), plus the 16-row sh_c scratch.
     constexpr int FAT_STAGES = 3;
-    size_t shared = FAT_STAGES * FAT_TILE_M * FAT_TILE_K * sizeof(half)
+    size_t shared = FAT_STAGES * tile_m * FAT_TILE_K * sizeof(half)
                   + FAT_STAGES * FAT_N_BLOCKS * FAT_PACKED_WORDS * sizeof(uint16_t)
                   + 16 * FAT_TILE_N * sizeof(float);
-    exl3_fat_gemm_kernel<scatter, paired><<<grid, block, shared, stream>>>(
+    exl3_fat_gemm_kernel<tile_m, scatter, paired><<<grid, block, shared, stream>>>(
         reinterpret_cast<const half*>(a.data_ptr()),
         reinterpret_cast<const uint16_t*>(packed.data_ptr()),
         paired ? reinterpret_cast<const uint16_t*>(packed_pair.data_ptr()) : nullptr,
@@ -386,7 +391,23 @@ void exl3_fat_gemm(
     check_common(a, packed, out, svh, K, mcg, mul1);
     TORCH_CHECK(out.size(0) == a.size(0) && out.size(1) == svh.numel(),
                 "out shape must be [M, N]");
-    launch<false>(a, packed, out, svh, at::Tensor(), at::Tensor());
+    launch<FAT_TILE_M, false>(a, packed, out, svh, at::Tensor(), at::Tensor());
+}
+
+void exl3_fat_gemm_m64(
+    at::Tensor a,
+    at::Tensor packed,
+    at::Tensor out,
+    at::Tensor svh,
+    int64_t K,
+    bool mcg,
+    bool mul1)
+{
+    check_common(a, packed, out, svh, K, mcg, mul1);
+    TORCH_CHECK(out.size(0) == a.size(0) && out.size(1) == svh.numel(),
+                "out shape must be [M, N]");
+    launch<FAT_TILE_M_SMALL, false>(
+        a, packed, out, svh, at::Tensor(), at::Tensor());
 }
 
 void exl3_fat_gemm_pair(
@@ -408,7 +429,37 @@ void exl3_fat_gemm_pair(
                 "paired output scales must have identical shapes");
     TORCH_CHECK(out.size(0) == a.size(0) && out.size(1) == 2 * svh_gate.numel(),
                 "paired out shape must be [M, 2N]");
-    launch<false, true>(
+    launch<FAT_TILE_M, false, true>(
+        a,
+        packed_gate,
+        out,
+        svh_gate,
+        at::Tensor(),
+        at::Tensor(),
+        packed_up,
+        svh_up);
+}
+
+void exl3_fat_gemm_pair_m64(
+    at::Tensor a,
+    at::Tensor packed_gate,
+    at::Tensor packed_up,
+    at::Tensor out,
+    at::Tensor svh_gate,
+    at::Tensor svh_up,
+    int64_t K,
+    bool mcg,
+    bool mul1)
+{
+    check_common(a, packed_gate, out, svh_gate, K, mcg, mul1);
+    check_common(a, packed_up, out, svh_up, K, mcg, mul1);
+    TORCH_CHECK(packed_gate.sizes() == packed_up.sizes(),
+                "paired packed tensors must have identical shapes");
+    TORCH_CHECK(svh_gate.sizes() == svh_up.sizes(),
+                "paired output scales must have identical shapes");
+    TORCH_CHECK(out.size(0) == a.size(0) && out.size(1) == 2 * svh_gate.numel(),
+                "paired out shape must be [M, 2N]");
+    launch<FAT_TILE_M_SMALL, false, true>(
         a,
         packed_gate,
         out,
@@ -477,5 +528,33 @@ void exl3_fat_gemm_scatter(
                 "routing tensors must have M elements");
     TORCH_CHECK(token_idx.device() == a.device() && route_weight.device() == a.device(),
                 "routing tensors must share a device with a");
-    launch<true>(a, packed, out, svh, token_idx, route_weight);
+    launch<FAT_TILE_M, true>(
+        a, packed, out, svh, token_idx, route_weight);
+}
+
+void exl3_fat_gemm_scatter_m64(
+    at::Tensor a,
+    at::Tensor packed,
+    at::Tensor out,
+    at::Tensor svh,
+    at::Tensor token_idx,
+    at::Tensor route_weight,
+    int64_t K,
+    bool mcg,
+    bool mul1)
+{
+    check_common(a, packed, out, svh, K, mcg, mul1);
+    TORCH_CHECK(out.size(1) == svh.numel(), "out N dimension mismatch");
+    TORCH_CHECK(token_idx.is_cuda() && route_weight.is_cuda(),
+                "routing tensors must be CUDA tensors");
+    TORCH_CHECK(token_idx.is_contiguous() && route_weight.is_contiguous(),
+                "routing tensors must be contiguous");
+    TORCH_CHECK(token_idx.scalar_type() == at::kLong, "token_idx must be int64");
+    TORCH_CHECK(route_weight.scalar_type() == at::kHalf, "route_weight must be float16");
+    TORCH_CHECK(token_idx.numel() == a.size(0) && route_weight.numel() == a.size(0),
+                "routing tensors must have M elements");
+    TORCH_CHECK(token_idx.device() == a.device() && route_weight.device() == a.device(),
+                "routing tensors must share a device with a");
+    launch<FAT_TILE_M_SMALL, true>(
+        a, packed, out, svh, token_idx, route_weight);
 }
diff --git a/overlay/exl3_fat_gemm.cuh b/overlay/exl3_fat_gemm.cuh
index d765b45..f61603e 100644
--- a/overlay/exl3_fat_gemm.cuh
+++ b/overlay/exl3_fat_gemm.cuh
@@ -11,6 +11,15 @@ void exl3_fat_gemm(
     bool mcg,
     bool mul1);
 
+void exl3_fat_gemm_m64(
+    at::Tensor a,
+    at::Tensor packed,
+    at::Tensor out,
+    at::Tensor svh,
+    int64_t K,
+    bool mcg,
+    bool mul1);
+
 void exl3_fat_gemm_pair(
     at::Tensor a,
     at::Tensor packed_gate,
@@ -22,6 +31,17 @@ void exl3_fat_gemm_pair(
     bool mcg,
     bool mul1);
 
+void exl3_fat_gemm_pair_m64(
+    at::Tensor a,
+    at::Tensor packed_gate,
+    at::Tensor packed_up,
+    at::Tensor out,
+    at::Tensor svh_gate,
+    at::Tensor svh_up,
+    int64_t K,
+    bool mcg,
+    bool mul1);
+
 void exl3_fat_swiglu_had(
     at::Tensor gate_up,
     at::Tensor out,
@@ -38,3 +58,14 @@ void exl3_fat_gemm_scatter(
     int64_t K,
     bool mcg,
     bool mul1);
+
+void exl3_fat_gemm_scatter_m64(
+    at::Tensor a,
+    at::Tensor packed,
+    at::Tensor out,
+    at::Tensor svh,
+    at::Tensor token_idx,
+    at::Tensor route_weight,
+    int64_t K,
+    bool mcg,
+    bool mul1);
diff --git a/overlay/patch_exl3_fat_kernel.py b/overlay/patch_exl3_fat_kernel.py
index 95bba9d..00a8bf4 100755
--- a/overlay/patch_exl3_fat_kernel.py
+++ b/overlay/patch_exl3_fat_kernel.py
@@ -40,9 +40,12 @@ def main() -> int:
         '    m.def("exl3_moe", &exl3_moe, "exl3_moe");',
         '    m.def("exl3_moe", &exl3_moe, "exl3_moe");\n'
         '    m.def("exl3_fat_gemm", &exl3_fat_gemm, "exl3_fat_gemm");\n'
+        '    m.def("exl3_fat_gemm_m64", &exl3_fat_gemm_m64, "exl3_fat_gemm_m64");\n'
         '    m.def("exl3_fat_gemm_pair", &exl3_fat_gemm_pair, "exl3_fat_gemm_pair");\n'
+        '    m.def("exl3_fat_gemm_pair_m64", &exl3_fat_gemm_pair_m64, "exl3_fat_gemm_pair_m64");\n'
         '    m.def("exl3_fat_swiglu_had", &exl3_fat_swiglu_had, "exl3_fat_swiglu_had");\n'
-        '    m.def("exl3_fat_gemm_scatter", &exl3_fat_gemm_scatter, "exl3_fat_gemm_scatter");',
+        '    m.def("exl3_fat_gemm_scatter", &exl3_fat_gemm_scatter, "exl3_fat_gemm_scatter");\n'
+        '    m.def("exl3_fat_gemm_scatter_m64", &exl3_fat_gemm_scatter_m64, "exl3_fat_gemm_scatter_m64");',
     )
     bindings.write_text(text)
     return 0
diff --git a/tests/test_exl3_overlay.py b/tests/test_exl3_overlay.py
index 531d80d..5344fb6 100644
--- a/tests/test_exl3_overlay.py
+++ b/tests/test_exl3_overlay.py
@@ -263,6 +263,9 @@ def _check_fat_kernel(device) -> None:
     assert hasattr(exllamav3_ext, "exl3_fat_gemm_scatter")
     assert hasattr(exllamav3_ext, "exl3_fat_gemm_pair")
     assert hasattr(exllamav3_ext, "exl3_fat_swiglu_had")
+    assert hasattr(exllamav3_ext, "exl3_fat_gemm_m64")
+    assert hasattr(exllamav3_ext, "exl3_fat_gemm_pair_m64")
+    assert hasattr(exllamav3_ext, "exl3_fat_gemm_scatter_m64")
 
     rows = 145
     in_f = out_f = 256
@@ -307,6 +310,11 @@ def _check_fat_kernel(device) -> None:
     assert direct_err < bound, (
         f"E2 direct vs reconstruct maxabs={direct_err} bound={bound}"
     )
+    direct_m64 = torch.empty_like(direct)
+    exllamav3_ext.exl3_fat_gemm_m64(
+        xh, trellis, direct_m64, svh, 4, True, False
+    )
+    assert torch.equal(direct, direct_m64), float((direct - direct_m64).abs().max())
 
     up_trellis = torch.randint(
         -30000,
@@ -345,6 +353,21 @@ def _check_fat_kernel(device) -> None:
     assert pair_err < pair_bound, (
         f"E2 pair vs reconstruct maxabs={pair_err} bound={pair_bound}"
     )
+    paired_m64 = torch.empty_like(paired)
+    exllamav3_ext.exl3_fat_gemm_pair_m64(
+        xh,
+        trellis,
+        up_trellis,
+        paired_m64,
+        svh,
+        up_svh,
+        4,
+        True,
+        False,
+    )
+    assert torch.equal(paired, paired_m64), float(
+        (paired - paired_m64).abs().max()
+    )
 
     gate_up = torch.randn(
         rows, 2 * out_f, dtype=torch.float32, device=device
@@ -392,6 +415,21 @@ def _check_fat_kernel(device) -> None:
     assert scatter_err < bound, (
         f"E2 scatter vs reconstruct maxabs={scatter_err} bound={bound}"
     )
+    scattered_m64 = torch.zeros_like(expected)
+    exllamav3_ext.exl3_fat_gemm_scatter_m64(
+        xh,
+        trellis,
+        scattered_m64,
+        svh,
+        token_idx,
+        route_weight,
+        4,
+        True,
+        False,
+    )
+    assert torch.equal(scattered, scattered_m64), float(
+        (scattered - scattered_m64).abs().max()
+    )
     print(
         f"exl3 E2 direct/scatter parity OK rows={rows} "
         f"direct={direct_err:.5f} pair={pair_err:.5f} "
