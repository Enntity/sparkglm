# Optional adaptive TP2 source preview

The [source package and reproducible profile](../research/adaptive-verification/README.md) retain the exact measured patch, graph sizes, source hashes, license boundaries and SSD composition order. This is opt-in; it does not change the default launcher or certify a newly built image.
